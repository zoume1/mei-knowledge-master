---
title: 文件的操作
date: 2025-11-18 16:32:00
permalink: /pages/e47080/
categories:
  - python
  - 基础使用
tags:
    -
---
## 文件对象的方法

open(): 创建一个file方法，默认是以只读模式打开

read(n):n表示从文件中读取的数据长度，默认文件整个内容

write（）：将指定内容写入文件

close（）：关闭文件

## 文件对象属性

文件名.name   返回要打开的文件的文件名

文件名.mode 返回文件的访问模式

文件名.closed  检测文件是否关闭  关闭了返回True

## 打开文件

### open()打开函数

```
f = open('102.txt','r', encoding='UTF-8')
# encoding的顺序不是第三位，所以不能用位置参数，用关键字参数直接指定
```

* name: 是要打开的目标文件名的字符串（可用包含文件缩在的具体路径）
* mode: 设置打开文件的模式（访问模式）：只读、写入、追加等
* encoding: 编码格式（推荐使用 UTF-8）


| 模式 | 描述                                                                                                               |
| ---- | ------------------------------------------------------------------------------------------------------------------ |
| r    | 以只读方式打开文件。文件的指针将会放在文件的开头。这是默认模式                                                     |
| w    | 打开一个文件只用于写入。如果改文件已存在则打开文件，并从头开始编辑，原有内容会被删除，如果该文件不存在，创建新文件 |
| a    | 打开一个文件用于追加。如果该文件已经存在，新的内容将会被写入到已有内容之后。如果该文件不存在，创建新文件进行写入   |

**注意**

此时的 f 是 open函数的文件对象，对象是Python中一种特殊的数据类型，拥有属性和方法，可以使用对象.属性或对象.方法对其进行访问

## 读写文件

### 读取文件文件

#### read()方法

```
f = open('102.txt')
print(f.read())
f.close()
```

#### readlines()方法

readlines可以按照行的方式把整个文件中的内容进行一次性读取，并且返回的是一个列表，其中每一行的数据为一个元素

```
f = open('python.txt')

content = f.readline()
print(f'第一行：{content}')

content = f.readline()
print(f'第二行：{content}')

f.close()
```


#### for循环读取文件行

```
for line in open("python.txt","r"):
  print(line)
# 没一个line临时变量，就记录了文件的一行数据
```

#### with open语法

```
with open("python.txt", "r") as f:
  f.readlines()
```

注意 代码执行完会自动调f.close（）方法，可以省略文件关闭步骤

### 文件写入


#### 写操作快速入门

```
# 1.打开文件
f = open('python.txt','w')

# 2.文件写入
f.write('hello world')

# 3.内容刷新
f.flush()
```


注意：

* 直接调用write,内容并未真正写入文件，而是会积攒在程序的内存中，称之为缓冲区
* 当调用flush的时候，内容会真正写入文件
* 这样做是避免频繁操作硬盘，导致效率下降（攒一堆，一次性写磁盘）

注意

* w模式，文件不存在，会创建新文件
* w模式，文件存在，会清空原有内容
* close()方法，带有flush()方法的功能


## 关闭文件

close() 方法，实例上面有展示

## 文件操作命令
